name: thử nghiệm đọc file

on:
  push:
    branches: [main]

jobs:
  thuc_hanh_doc_file:
    runs-on: ubuntu-latest

    steps:
      - name: Bước 1 - xin chào
        run: echo "Hello Dat"

      - name: Bước 2 - Tải code
        uses: actions/checkout@v6.0.2

      - name: Bước 3 - Đọc file
        run: cat README.md
